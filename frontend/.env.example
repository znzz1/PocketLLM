# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000/chat/ws

# WebSocket will connect directly to backend (not through Next.js BFF)
# This is because Next.js API Routes do not support WebSocket upgrades
# See: Architecture Document Section 3.2.2
